services:
  api:
    build: .
    container_name: fastapi_ecommerce_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: "1"
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
